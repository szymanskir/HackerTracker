if (require("renv", character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)) {
  source("renv/activate.R")
} else {
  warning("Renv is used in this package, please install renv using `install.packages`!")
}
